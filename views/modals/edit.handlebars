<div id="editModal" class="modal">
  <div class="modal-form">
    <h2>Edit Reservation</h2>
    <p class="subtitle">Update the details of your reservation below.</p>

    <form id="editReservationForm" method="POST">
      <input type="hidden" name="reservationId" id="editReservationId" />

      <div class="form-group">
        <label>Laboratory</label>
        <select name="lab" id="editLab" required>
          <option disabled selected>Select a lab</option>
          {{#each labs}}
            <option value="{{this._id}}">{{this.lab}}</option>
          {{/each}}
        </select>
      </div>

      <div class="form-group">
        <label>Seat</label>
        <select name="seat" id="editSeat" required>
          <option disabled selected>Loading seats...</option>
        </select>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label>Date</label>
          <input type="date" name="date" id="editDate" required />
        </div>
        <div class="form-group">
          <label>Start Time</label>
          <input type="time" name="start_time" id="editStartTime" required step="1800" />
        </div>
        <div class="form-group">
          <label>End Time</label>
          <input type="time" name="end_time" id="editEndTime" required step="1800" />
        </div>
      </div>

      <div class="form-group">
        <label>Purpose</label>
        <input type="text" name="purpose" id="editPurpose" required />
      </div>

      <div class="register-actions">
        <button type="submit" class="btn-large">Save Changes</button>
        <button type="button" id="cancelEdit" class="btn-cancel-register" style="border:none; background:none;">Cancel</button>
      </div>
    </form>
  </div>
</div>
